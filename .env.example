# ── Server ──────────────────────────────────
PORT=5000

# ── Security (CHANGE THIS before deploying!) ─
JWT_SECRET=change_this_to_a_long_random_string_abc123xyz

# ── CORS (your frontend URL after deploy) ────
# Local:
FRONTEND_URL=*
# Production example:
# FRONTEND_URL=https://your-app.netlify.app
